<form method="post" action="{{ path('app_demand_relation_delete', {'id': demand_relation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demand_relation.id) }}">
    <button class="btn">Delete</button>
</form>
