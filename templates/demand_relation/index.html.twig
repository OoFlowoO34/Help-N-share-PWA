{% extends 'base.html.twig' %}

{% block title %}DemandRelation index{% endblock %}

{% block body %}
    <h1>DemandRelation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for demand_relation in demand_relations %}
            <tr>
                <td>{{ demand_relation.id }}</td>
                <td>
                    <a href="{{ path('app_demand_relation_show', {'id': demand_relation.id}) }}">show</a>
                    <a href="{{ path('app_demand_relation_edit', {'id': demand_relation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_demand_relation_new') }}">Create new</a>
{% endblock %}
